import styled from "styled-components"
import tw from "twin.macro"

export const Tailwind = styled.div`
  ${tw`font-sans text-lg`}
`

export const Container = styled.section`
  ${tw`container mx-auto px-5`}
`;